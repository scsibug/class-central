{% extends 'ClassCentralSiteBundle::base.html.twig' %}

{% block content %}
<h1>Offering creation</h1>

<form action="{{ path('offering_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('offering') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}